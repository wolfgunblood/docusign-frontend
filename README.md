# Project Name

Docusign frontend assignment

## Installation

To run this project locally, follow these steps:

Extract Zip folder

Navigate to the project directory:

cd project-directory

Install the dependencies using npm:

npm install

Usage
To start the application, use the following command:
npm run dev

The application will now be running on http://localhost:5173. Open your web browser and navigate to this URL to access the application.
